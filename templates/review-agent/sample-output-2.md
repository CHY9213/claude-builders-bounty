# PR Review Report

**[BOUNTY $100] HOOK: Pre-tool-use hook that blocks destructive bash commands**

Link: https://github.com/claude-builders-bounty/claude-builders-bounty/pull/2154

## Summary

> ## Summary

A Claude Code `pre-tool-use` hook in Python that intercepts and blocks dangerous bash commands before execution.

## Acceptance Checklist

- [x] Hook follows Claude Code hooks format (`~/.claude/hooks/`)
- [x] Blocks: `rm -rf`, `DROP TABLE`, `git push --force`, `TRUNCATE`, `DELETE FROM` without WHERE clause
- [x] Logs every blocked attempt to `~/.claude/hooks/blocked.log` with timestamp, command, project path
- [x] Displays a clear message to Claude explaining why the command was blo

This PR changes **4** files (+291/-0 lines).

### Changed Files

| File | + | - |
|------|---|---|
| `CLAUDE.md` | +77 | -0 |
| `changelog.sh` | +41 | -0 |
| `templates/hooks/README.md` | +52 | -0 |
| `templates/hooks/pre-tool-use.py` | +121 | -0 |

## High Risk Issues

- **Destructive filesystem operation** in `templates/hooks/README.md`
  ```
  | `rm -rf` / `rm -fr` | Recursive forced deletion — irreversible |
  ```
- **Destructive database operation** in `templates/hooks/README.md`
  ```
  | `DROP TABLE` / `DROP DATABASE` / `DROP SCHEMA` | Database destruction |
  ```
- **Destructive database operation** in `templates/hooks/README.md`
  ```
  | `DROP TABLE` / `DROP DATABASE` / `DROP SCHEMA` | Database destruction |
  ```
- **Force push may rewrite history** in `templates/hooks/README.md`
  ```
  | `git push --force` / `git push -f` | History rewriting |
  ```
- **Destructive filesystem operation** in `templates/hooks/README.md`
  ```
  [2026-05-28 01:45:00] BLOCKED | project=/home/user/myapp | reason=Recursive forced deletion | cmd=rm -rf /data
  ```
- **Destructive filesystem operation** in `templates/hooks/pre-tool-use.py`
  ```
  - rm -rf (recursive forced deletion)
  ```
- **Destructive database operation** in `templates/hooks/pre-tool-use.py`
  ```
  - DROP TABLE (database destruction)
  ```
- **Force push may rewrite history** in `templates/hooks/pre-tool-use.py`
  ```
  - git push --force / --force-with-lease (history rewriting)
  ```
- **Bulk data deletion** in `templates/hooks/pre-tool-use.py`
  ```
  - TRUNCATE (bulk data deletion)
  ```
- **Destructive filesystem operation** in `templates/hooks/pre-tool-use.py`
  ```
  ("rm -rf", "Recursive forced deletion is irreversible. Use 'trash' or 'rm' with explicit file paths."),
  ```
- **Destructive database operation** in `templates/hooks/pre-tool-use.py`
  ```
  ("DROP TABLE", "Dropping database tables is destructive. Use migrations or backup first."),
  ```
- **Destructive database operation** in `templates/hooks/pre-tool-use.py`
  ```
  ("DROP DATABASE", "Dropping databases is destructive and irreversible."),
  ```
- **Bulk data deletion** in `templates/hooks/pre-tool-use.py`
  ```
  ("TRUNCATE", "TRUNCATE permanently removes all rows. Use DELETE with a WHERE clause, or backup first."),
  ```
- **Force push may rewrite history** in `templates/hooks/pre-tool-use.py`
  ```
  ("git push --force", "Force-pushing rewrites shared history. Use 'git push --force-with-lease' with caution and team coo
  ```

## Medium Risk / Concerns

- **Stray console.log debug output** in `CLAUDE.md`
  ```
  - [ ] No `console.log` in production — use a logger utility
  ```
- **Possible debug print statement** in `templates/hooks/pre-tool-use.py`
  ```
  print(json.dumps(result))
  ```

## Improvement Suggestions

1. No test files detected. Consider adding tests.

## Review Confidence

**❌ Confidence: Low**

Significant concerns detected. Requires thorough human review.

---
_Generated by claude-review-agent_